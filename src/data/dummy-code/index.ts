export const jsDummy =
  'class Car {\n  constructor(driver) {\n    this.driver = driver;\n    this.speed = 0;\n  }\n  \n  drive(speed) {\n    this.speed += speed;\n    console.log(`${this.driver} drives with speed ${this.speed}.`);\n  }\n}\n\nclass Driver{\n  constructor(name, surname){\n    this.name = name;\n    this.surname: surnma;\n  }\n  \n  getDriver(){\n    return this.name + this.surname;\n  }\n}\nconst driver = new Driver("Ahmet", "İlhan")\nconst car = new Car(driver);\ncar.drive(900); // If I\'m in the driver\'s seat, I don\'t know your king.'
